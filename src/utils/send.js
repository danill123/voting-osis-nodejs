const db = require('./db')

const send = (app) => {
    // upload data & gambar calon
    app.post('/plus_calon', (req, res) => {
    
    })

    app.get('/pilih', (req, res) => {
        
    })
}

module.exports = send;